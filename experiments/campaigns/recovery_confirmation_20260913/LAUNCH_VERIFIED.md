# Recovery confirmation: акт запуска

- Запрос пользователя: план на два дня и автономные эксперименты на 9 часов.
- Запуск: **13 сентября 2026, 01:27:24 МСК**.
- Deadline новых вычислений: **13 сентября, 10:27:24 МСК**; до120 секунд
  на остановку своих workers, затем CPU-анализ завершённых случаев.
- Canonical project: `/home/jovyan/shares/SR006.nfs2/spiridonov/malnev_world_model/YSDA_WORD_MODELS_PP`.
- Dispatcher PID: `3085871`, detached session, локальная SSH-сессия ему не нужна.
- Frozen config SHA256:
  `a3c73f4b93ee7249e12536104269aa6ab35d603e5e8e5fdc395426c5f6e5eb15`.
- На старте технического replay: GPU1/PID3086153, GPU0/PID3086157,
  GPU6/PID3086158. Это временное назначение, следующие batches динамические.
- GPU4-5 заняты чужими процессами и не использованы при admission.
- Source/runtime/artifact/replay hashes проверены на сервере перед запуском.
- План: 9 technical branches,512 main outcomes,768 timing outcomes.
  Всего1289; полное выполнение зависит от доступных ресурсов.

Это подтверждение работающего dispatcher и технических GPU-workers,
**не заявление о завершении main или положительном результате**. Main
автоматически разрешён только после точного воспроизведения всех9 старых
контролей; научный отбор метода по SR ночью отсутствует.

## Основная серия разрешена

В **01:32:45 МСК** replay gate пройден, начата фаза `main`.
Все9 старых ветвей побитово совпали по действиям, конечному состоянию и
per-step signals. MP4 technical replay декодированы. **78 CPU-тестов**
прошли локально и на сервере, XML сохранены рядом с config.

В начале main были допущены GPU0-7, по одному worker на GPU. Прежние
сторонние GPU4-5 процессы к этому моменту завершились; admission повторно
проверил отсутствие CUDA-процессов и два idle измерения. Ничьи процессы
не завершались нашими командами. GPU membership может измениться между batches.

Main worker PIDs на этом срезе: 3098415,3098418,3098421,3098422,
3098425,3098430,3098431,3098432. Актуальный набор всегда в `active` статуса.
Начальный ETA по нескольким smoke jobs включает холодную загрузку модели
и не является надёжной оценкой длительности основной фазы; deadline фиксирован.

Первые8 невмешанных H16 baseline-файлов сохранены. Дополнительно проверены
две новые main regrasp-ветви: `x0.2_t5_i25` (149 кадров, t0-148) и
`x0.3_t1_i25` (281 кадр, t0-280). Оба MP4 полностью декодируются;
frame_t, число физических действий и исполненные префиксы выбранных
candidate chunks совпадают. Это техническая проверка полноты видео,
не промежуточный отбор метода по SR.

## Файлы

- `config.json`: frozen методика, jobs, seeds, контрольные суммы.
- `budget.json`: неизменяемый при resume deadline.
- `sequence_status.json`: фактический live-статус на сервере. Локальная копия
  является только последним скачанным срезом.
- `batches/*.log`: stdout и ошибки отдельных workers.
- `analysis/smoke/summary.json`: gate точного воспроизведения.
- `analysis/main/RESULTS.md`, `paired_effects.csv`, `videos.html`: основной результат.
- `analysis/timing/`: отдельная sensitivity-проверка t56/t88.

```bash
ssh mlspace-sr006 'cd /home/jovyan/shares/SR006.nfs2/spiridonov/malnev_world_model/YSDA_WORD_MODELS_PP && .venv-cosmos/bin/python scripts/run_recovery_confirmation.py --status'
```

`completed` означает завершение всех фаз и анализа. `partial` означает
остановку по бюджету/сигналу, не отрицательный научный исход. `failed` требует
проверки указанного error/log; повторного бесконечного запуска нет.

Полный дизайн и двухдневный план: [RECOVERY_TWO_DAY_PLAN_20260913.md](../../RECOVERY_TWO_DAY_PLAN_20260913.md).
